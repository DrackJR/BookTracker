@page
@model BookTracker.Pages.Author.EditModel
@{
    ViewData["Title"] = "Редактировать автора";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Редактировать автора</h4>
                </div>
                <div class="card-body">
                    <form method="post">
                        <input type="hidden" asp-for="Author.Id" />

                        <div class="mb-3">
                            <label asp-for="Author.Name" class="form-label">Имя автора</label>
                            <input asp-for="Author.Name" class="form-control" />
                            <span asp-validation-for="Author.Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Author.Biografy" class="form-label">Биография</label>
                            <textarea asp-for="Author.Biografy" class="form-control" rows="5"></textarea>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-page="Index" class="btn btn-secondary">Назад</a>
                            <button type="submit" class="btn btn-success">Сохранить</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}